{% if data is not null %}
    <img src="{{ path('kmj_upload_download', {id: brand.data })}}" {{ attributes }}/>
{% else %}
    <img src="{{ asset('bundles/upload/images/no-image.png')}}" {{ attributes }}/>
{% endif %}